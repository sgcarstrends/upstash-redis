{
  "name": "@sgcarstrends/upstash-redis",
  "description": "Upstash Redis database in a team",
  "main": "index.ts",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "@pulumi/pulumi": "^3.130.0",
    "@upstash/pulumi": "^0.3.14",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.1.0",
    "prettier": "^3.3.3"
  }
}
